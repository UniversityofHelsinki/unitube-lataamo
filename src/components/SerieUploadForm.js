import React, { useEffect, useState } from 'react';
import { connect } from 'react-redux';
import { Translate } from 'react-redux-i18n';

const SerieUploadForm = (props) => {
    return(
        <div>
        </div>
    );
};

export default SerieUploadForm;